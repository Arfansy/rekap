document.addEventListener('DOMContentLoaded', () => {
    // This script can be used for various interactive elements later.
    // Examples:
    // 1. Smooth scrolling to sections
    // 2. Highlighting active navigation link based on scroll position
    // 3. Simple image gallery/lightbox for article images
    // 4. Comment section functionality (requires backend)
    // 5. "Back to Top" button

    console.log("Baca Selengkapnya page loaded successfully!");

    // Example: Add a simple scroll-to-top button
    // (Uncomment and add corresponding HTML for a button if desired)
    /*
    const scrollToTopBtn = document.createElement('button');
    scrollToTopBtn.textContent = '↑ Top';
    scrollToTopBtn.classList.add('scroll-to-top');
    document.body.appendChild(scrollToTopBtn);

    window.addEventListener('scroll', () => {
        if (window.scrollY > 300) {
            scrollToTopBtn.style.display = 'block';
        } else {
            scrollToTopBtn.style.display = 'none';
        }
    });

    scrollToTopBtn.addEventListener('click', () => {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });
    */
});